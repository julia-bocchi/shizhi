export interface WorkoutRecord {
  id: string;
  date: string;
  name: string;
  calories: number;
  durationSeconds: number;
  exerciseId: string;
  planId: string;
  distanceKm: number;
  count: number;
}
